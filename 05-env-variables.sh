#!/bin/bash

# export COURSE="DevOps with AWS" you can pass this argument into outside the script this is called environment variables
# but above only works this process is running once close the terminal or server the process ends then that environment variable wont work
# if you want to perminant to that variable you will add that variable into .bashrc file

echo $COURSE